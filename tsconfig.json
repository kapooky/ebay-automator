{
  "ts-node": {
    // these options are overrides used only by ts-node
    // same as the --compilerOptions flag and the TS_NODE_COMPILER_OPTIONS environment variable
    "compilerOptions": {
      "module": "commonjs"
    }
  },
  "compilerOptions": {
    "module": "esnext",
    "target": "es5",
    //"outDir": "out",
    "skipLibCheck": true,
    "baseUrl": "."
  }
}

//Cool path mapping https://stackoverflow.com/questions/43281741/how-to-use-paths-in-tsconfig-json