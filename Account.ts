export class Account {
    listings;
    name;

    constructor(listings, name, credentials) {
        this.listings = [];
        this.name = name;
        this.initializeCredentials(credentials)
    }

    private initializeCredentials(credentials) {

    }

    private handleOrder() {

    }
    private initailizeLoop(){

    }


}
